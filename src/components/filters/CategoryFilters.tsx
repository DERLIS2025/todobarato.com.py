import Link from "next/link";import { categories } from "@/data/categories";import { BrandFilter } from "./BrandFilter";import { ColorFilter } from "./ColorFilter";import { PriceFilter } from "./PriceFilter";
export function CategoryFilters(){return <aside className="card p-5"><h3 className="font-black">Filtros</h3><div className="mt-5 grid gap-5"><div><h4 className="mb-2 font-bold">Categoría</h4>{categories.map(c=><Link className="block py-1 text-sm hover:text-primary" href={`/categoria/${c.slug}`} key={c.slug}>{c.name}</Link>)}</div><PriceFilter/><BrandFilter/><ColorFilter/><div><h4 className="mb-2 font-bold">Disponibilidad</h4><label className="block text-sm"><input type="checkbox"/> En stock</label></div><div><h4 className="mb-2 font-bold">Etiquetas</h4><div className="flex flex-wrap gap-2">{['oferta','pack','nuevo','regalo'].map(t=><span className="rounded-full bg-surface px-3 py-1 text-xs font-bold text-textSecondary" key={t}>{t}</span>)}</div></div></div></aside>}
