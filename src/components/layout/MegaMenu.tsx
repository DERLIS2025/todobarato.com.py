import Link from "next/link";import { mainNav } from "@/data/navigation";import { categories } from "@/data/categories";
export function MegaMenu(){return <div className="hidden border-b bg-white lg:block"><div className="container-page flex items-center gap-6"><div className="bg-brand-600 px-5 py-3 font-black text-white">☰ Todas las categorías</div><nav className="flex gap-5 text-sm font-bold">{mainNav.map(n=><Link className="hover:text-brand-600" href={n.href} key={n.href}>{n.label}</Link>)}</nav><div className="ml-auto text-sm text-slate-500">{categories.length} departamentos activos</div></div></div>}
