import { ProductCard } from "@/components/product/ProductCard";import { searchProducts } from "@/lib/productUtils";
export default async function SearchPage({searchParams}:{searchParams:Promise<{q?:string}>}){const {q=''}=await searchParams;const list=searchProducts(q);return <div className="container-page mt-8"><h1 className="text-3xl font-black">Buscar</h1><p className="mt-2 text-primaryDark/60">Resultados para “{q || 'todos'}”</p><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{list.map(p=><ProductCard product={p} key={p.id}/>)}</div></div>}
